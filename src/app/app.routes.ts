import { Routes } from '@angular/router';
import { ScratchCardComponent } from './components/scratch-card/scratch-card.component';

export const routes: Routes = [{ path: '', component: ScratchCardComponent }];
